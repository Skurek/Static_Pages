<%= form_for(@post, :remote => true) do |f| %>
    <div class="field">
      <%= f.text_area :body %>
    </div>
    <div class= "button">
      <%= f.submit class: "btn btn-large btn-primary", value: "Wyslij" %>
    </div>
<% end %>

